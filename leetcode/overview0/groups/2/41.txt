Intution:
========
start: 10:10

1. 桶 -> 不行
2. O(N)的时间复杂度, 二分之类的统统不是
3. 连续数字的问题: 我见过类似的!!! 用集合?

我找1, 同时把所有的负数都变成1 => 找不到1? 返回1
遍历每个数a[i]=x, 如果这个数字比数组的长度要小, 就把a[x]设为负数
遍历整个数组, 找到第一个不是负数的数字, 返回

这里真正有用的提示是约束条件: O(N)的时间+常数级空间
这样的题目, 我也见过类似的, 通常都是利用下标搞事情, 利用值的正负来做标记

end: 10:17

Discuss:
=======

跟我的一样
