/**
 * D
 */

#include <algorithm>
#include <cassert>
#include <cstdio>
#include <cstring>
#include <map>
#include <stack>
#include <string>
#include <vector>

#include "common.h"

using namespace std;

#define N 200002

char s[N];
int lefts[N], rghts[N];

bool read(void) {
	return true;
}

void solve(void) {
}

int main(void) {
	#ifdef DEBUG
	//freopen("Inputs/D", "r", stdin);
	setbuf(stdout, NULL);
	#endif

	read();
	solve();

	return 0;
}
